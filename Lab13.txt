Lab #13 Blind SQL injection with time delays

Vulnerable parameter - tracking cookie

End Goal:
    To prove that the field is vulnerable to blind SQLi (time based).
------------------------------------------------------------------------

Steps:

1. We have to make the query by which the server delays the response by 10 sec.

=> for that we have to check on all the databases for which we are using rn.

' || (select sleep(10))--       => MySQL(❌)

' || (select pg_sleep(10))--    => PostgreSQL(✅)

we got all this from the sqli cheat sheet from portswigger.
